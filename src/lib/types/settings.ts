// Tipos para configuraciones
/*import type { Band, ExtraRate, InitialRate, VehicleType } from "./database";

export interface GetVehicleTypesResponse {
	ok: boolean;
	data?: {
		vehicleTypes: VehicleType[];
	};
	error?: unknown;
}

export interface CreateVehicleTypeRequest {
	name: string;
	isActive?: boolean;
}

export interface CreateVehicleTypeResponse {
	ok: boolean;
	data?: {
		vehicleType: VehicleType;
	};
	error?: unknown;
}

export interface GetBandsResponse {
	ok: boolean;
	data?: {
		bands: Band[];
	};
	error?: unknown;
}

export interface CreateBandRequest {
	name: string;
	startHour: number;
	endHour: number;
	isActive?: boolean;
}

export interface CreateBandResponse {
	ok: boolean;
	data?: {
		band: Band;
	};
	error?: unknown;
}

export interface GetInitialRatesResponse {
	ok: boolean;
	data?: {
		initialRates: InitialRate[];
	};
	error?: unknown;
}

export interface CreateInitialRateRequest {
	vehicleTypeId: number;
	amount: number;
	validFrom: Date;
	validTo?: Date;
}

export interface CreateInitialRateResponse {
	ok: boolean;
	data?: {
		initialRate: InitialRate;
	};
	error?: unknown;
}
*/
